[pytest]
testpaths = tests
addopts = -m "not postgres and not migration and not concurrency"
markers =
    unit: fast isolated test that does not require PostgreSQL semantics
    postgres: test that connects only to the approved PostgreSQL test branch
    migration: test that inspects or changes PostgreSQL migration state
    concurrency: bounded PostgreSQL transaction or race-condition test
