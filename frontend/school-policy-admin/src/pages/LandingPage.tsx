import { CTA, SectionHeading } from '@/components/marketing/MarketingPrimitives'
import { BRAND_NAME } from '@/content/brand'
import { supportedLanguages } from '@/i18n/languages'
import { useLandingLanguage } from '@/i18n/useLandingLanguage'

export function LandingPage() {
  const { language, setLanguage, text } = useLandingLanguage()
  return <div>
    <section className="overflow-hidden bg-[#10211d] text-white"><div className="mx-auto max-w-7xl px-5 py-20 sm:px-8 lg:py-28"><label className="mb-10 inline-flex items-center gap-2 text-sm text-lime-100">Language<select aria-label="Language" value={language} onChange={(event) => setLanguage(event.target.value as typeof language)} className="rounded-lg border border-lime-200/30 bg-[#10211d] px-2 py-1 text-white">{supportedLanguages.map((option) => <option key={option.code} value={option.code}>{option.label}</option>)}</select></label><p className="text-xs font-bold uppercase tracking-[.2em] text-lime-300">{text('hero.eyebrow', 'Offline-first school device governance')}</p><h1 className="mt-5 max-w-3xl text-5xl font-bold leading-[.96] tracking-[-.05em] sm:text-7xl">{text('hero.title', 'Keep the school day in focus, even when the network cannot.')}</h1><p className="mt-7 max-w-xl text-lg leading-8 text-white/70">{text('hero.body', `${BRAND_NAME} gives Ugandan secondary schools an accountable control surface for phone-use policies on school-owned Android devices.`)}</p><div className="mt-9 flex flex-wrap gap-3"><CTA href="/product">{text('hero.explore', 'Explore the product')}</CTA><CTA href="/login" secondary>{text('hero.admin', 'Open admin console')}</CTA></div><p className="mt-6 max-w-xl text-xs text-white/50">{text('hero.capabilities', 'Capabilities shown are based on the current repository and clearly marked where external Android verification remains required.')}</p></div></section>
    <section className="mx-auto max-w-7xl px-5 py-20 sm:px-8"><SectionHeading eyebrow="A focused control surface" title={text('capabilities.title', 'Four connected capabilities for accountable device use.')}>{text('capabilities.body', `${BRAND_NAME} connects administrator policy decisions with device status, acknowledgements, and security-relevant evidence.`)}</SectionHeading></section>
    <section className="bg-[#e5f0dc] px-5 py-20 sm:px-8"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow={text('how.eyebrow', 'How it works')} title={text('how.title', 'A policy lifecycle designed around real school conditions.')} /><div className="mt-10 grid gap-4 md:grid-cols-4">{[['01', 'Create'], ['02', 'Assign'], ['03', 'Enforce'], ['04', 'Reconcile']].map(([number, title]) => <article key={number} className="border-t border-emerald-900/20 pt-5"><span className="text-sm font-bold text-emerald-800">{number}</span><h3 className="mt-8 text-xl font-bold">{title}</h3></article>)}</div></div></section>
    <section className="bg-slate-950 px-5 py-20 text-white sm:px-8"><div className="mx-auto max-w-7xl"><p className="text-sm font-semibold text-lime-300">{text('status.eyebrow', 'Transparent PoC status')}</p><h2 className="mt-3 text-3xl font-bold sm:text-5xl">{text('status.title', 'A credible academic foundation, clearly scoped.')}</h2><p className="mt-5 max-w-2xl leading-8 text-white/70">{text('status.body', 'The repository contains the React admin portal and Flask contracts. Android DPC sources, device certification, rollout, and production readiness require separate verification.')}</p></div></section>
  </div>
}
