import { ArrowRight, CheckCircle2 } from 'lucide-react'
import { Link } from 'react-router-dom'
import { BRAND_NAME } from '@/content/brand'
export function Eyebrow({ children }: { children: React.ReactNode }) { return <p className="text-xs font-bold uppercase tracking-[.18em] text-emerald-700">{children}</p> }
export function SectionHeading({ eyebrow, title, children }: { eyebrow?: string; title: string; children?: React.ReactNode }) { return <div className="max-w-3xl">{eyebrow && <Eyebrow>{eyebrow}</Eyebrow>}<h2 className="mt-3 text-3xl font-bold tracking-tight sm:text-5xl">{title}</h2>{children && <p className="mt-5 text-lg leading-8 text-slate-600">{children}</p>}</div> }
export function CTA({ href, children, secondary = false }: { href: string; children: React.ReactNode; secondary?: boolean }) { return <Link to={href} className={`inline-flex items-center gap-2 rounded-full px-5 py-3.5 text-sm font-bold ${secondary ? 'border border-slate-300 bg-white text-slate-800' : 'bg-emerald-900 text-white'}`}>{children}<ArrowRight className="size-4" /></Link> }
export function Status({ children }: { children: React.ReactNode }) { return <span className="inline-flex items-center gap-2 rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1.5 text-xs font-semibold text-emerald-900"><CheckCircle2 className="size-3.5" />{children}</span> }
export function FeatureCard({ title, text, href }: { title: string; text: string; href?: string }) { return <article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><Status>{BRAND_NAME} capability</Status><h3 className="mt-5 text-xl font-bold">{title}</h3><p className="mt-3 leading-7 text-slate-600">{text}</p>{href && <Link to={href} className="mt-5 inline-flex font-bold text-emerald-800">Explore capability <ArrowRight className="ml-2 size-4" /></Link>}</article> }
