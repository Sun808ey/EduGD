# Public build-time configuration only. No database URLs or server secrets.
# Production requires https://<approved-api-host>/api/v1.
VITE_API_BASE_URL=
# Optional public ingest DSN, never a Sentry authentication token.
VITE_SENTRY_DSN=
